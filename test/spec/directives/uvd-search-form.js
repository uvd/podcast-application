'use strict';

describe('Directive: uvdSearchForm', function () {

    // load the directive's module
    beforeEach(module('uvdAngularPodcastTutorialApp'));

    var element,
        scope;

    beforeEach(inject(function ($rootScope) {
        scope = $rootScope.$new();
    }));

});
